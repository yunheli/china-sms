language: node_js
node_js:
- '0.11'
- '0.10'
script:
- echo $FLOW
addons:
  artifacts:
    artifacts: true
    paths:
    - $(git ls-files -o | tr "\n" ":")
    - $(ls /var/log/*.log | tr "\n" ":")
    - $HOME/some/other/thing.log